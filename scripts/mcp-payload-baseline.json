{
  "maxBytes": 28355,
  "reason": "25 tools: baseline is measured as the maximum UTF-8 JSON byte length across zh-CN and en tools/list projections, so CI and developer machine locales cannot change the ratchet result. The payload includes the 15 collapsed tools, 7 preserved M2 semantic editing tools, T1 layout.read/layout.write projections, and localized human titles for all nine semantic tools. 2026-09-07 (+308 bytes, 28047 → 28355): the agent-artifact node kind joins the canvas write surface — one new value in canvasNodeKindSchema plus the `artifact` {fileType, content} object that carries a hand-written file, and a reworded `prompt` description saying artifact nodes send an empty string. This is a real new capability reaching external hosts, not description drift: the whole point of the ratchet is that prose creeps while capabilities are supposed to be deliberate, so the increase is recorded with what bought it. Subsequent changes remain shrink-only."
}
