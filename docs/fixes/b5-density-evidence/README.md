# B5 真机证据

隔离 profile：`b5-density-humUZH`（系统临时目录）。主进程构建来自本工作树；渲染使用本地 Vite。所有用户旅程用界面点击/输入/文件选择完成，没有向应用 store 灌状态，没有读取真实用户资料库或调用收费模型。基线进程拍摄导入后，正常冷重启加载新主进程；不是重启绕过卡点。

- DC21：真实创建图片节点并输入长提示词，before 占位和编辑器重复全文；after 占位只有下一步。image/video/model3d 投影回归先红后绿。
- C09：真实侧栏聚焦导入图片，before 保留低倍率（本次真机50%），after 100%。20% 与高宽比/超大节点边界由单测覆盖；概览内核和最低倍率不变。
- C53：旧实现两次导入 test-upload.png 产生两条；冷重启新实现后再次导入仍仅两条，未删除已有重复。新图片 hires-detail-1024x1792.png 连续导入两次只有一条，素材库合计3项；画布允许主动多次使用同素材。
- DC24：通用/AI策略/权限/hosts 与预览空轨改前改后截图。选择效果、隐私告知、独立授权开关及轨道落点均保留。
- DC23：在真实设置页连续检查文件夹5次，全局通知0，原地反馈保留。真实Mantine store回归覆盖对象隔离、重复计数和更新回调。
- DC22：真实导入图片→预览来源点击贴尾→导出3秒MP4→任务中心显示完成与“查看成片”；点击后无动作错误。`dc22-real-export.png`。

`docs/plan/2026-09-10-b5-density-evidence/component-evidence` 是另行标明的 Electron 生产组件证据：精确基线源码+脱敏props，覆盖DC22失败/成功、B4显示名、DC23重复、DC24合法分镜。它不是用户端到端旅程。B4按钮仍遵守既有40%宽度省略，完整动作在title；未改变toast布局。

人眼检查：已逐组查看前后图片，结果文字可读；设置冗余显著减少；任务结果有明确后续动作。截图期间有选择器未匹配/动画等待失败，按真实DOM更正定位后继续，没有修改产品超时、测试断言或注入状态。
