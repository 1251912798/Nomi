{
  "schema_version": 3,
  "id": "2026-09-10-c09-readable-focus",
  "problem_type": "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged.",
  "symptom": "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged.",
  "direct_cause": "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged.",
  "class_root": "The shared presentation boundary does not enforce task-oriented, readable output.",
  "affected_population": [
    "Existing and new projects, zh-CN and en"
  ],
  "scope_paths": [
    "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
    "src/workbench/generationCanvas/reactFlow/useGenerationCanvasReactFlowEffects.ts"
  ],
  "entry_points": [
    "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
    "src/workbench/generationCanvas/reactFlow/useGenerationCanvasReactFlowEffects.ts"
  ],
  "external_sources": [],
  "internal_only_reason": "Application presentation policy; no dependency or external protocol change.",
  "invariants": [
    "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged."
  ],
  "generality_proof": "All checked consumers converge on the declared shared presentation boundary.",
  "shared_boundaries": [
    {
      "path": "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
      "symbol": "resolveCanvasFocusZoom",
      "responsibility": "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged."
    }
  ],
  "same_class_entry_points": [
    {
      "path": "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
      "entry_point": "resolveCanvasFocusZoom",
      "disposition": "enforced",
      "evidence": "Repository scan confirms the shared presentation policy applies here."
    },
    {
      "path": "src/workbench/generationCanvas/reactFlow/useGenerationCanvasReactFlowEffects.ts",
      "entry_point": "consumer",
      "disposition": "enforced",
      "evidence": "Repository scan confirms the shared presentation policy applies here."
    }
  ],
  "recurrence": {
    "classification": "recurring",
    "reason": "Every new or restored project can reach this presentation.",
    "same_class_scan": [
      "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
      "src/workbench/generationCanvas/reactFlow/useGenerationCanvasReactFlowEffects.ts"
    ]
  },
  "prevention": {
    "kind": "centralized-boundary",
    "enforcement_path": "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
    "invariant": "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged.",
    "failure_mode": "Keep actionable constraints visible; omit redundant presentation.",
    "exception_policy": "none",
    "strategy": "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged.",
    "artifacts": [
      "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
      "src/workbench/generationCanvas/reactFlow/useGenerationCanvasReactFlowEffects.ts"
    ]
  },
  "regression_tests": [
    "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.test.ts"
  ],
  "class_regression_tests": [
    "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.test.ts"
  ],
  "legacy_paths": {
    "status": "removed",
    "removed_paths": [
      "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
      "src/workbench/generationCanvas/reactFlow/useGenerationCanvasReactFlowEffects.ts"
    ],
    "rationale": "Replace old presentation in place; no parallel path."
  },
  "dependency_lifecycle": {
    "decision": "not-applicable",
    "rationale": "Internal presentation invariant; dependencies unchanged.",
    "exit_criteria": []
  },
  "migration": "No persisted schema changes. Existing projects render with the same rule.",
  "residual_risks": [
    "Real Electron before/after verification pending."
  ],
  "invariant_owner_layer": {
    "layer": "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.ts",
    "tests": [
      "src/workbench/generationCanvas/reactFlow/focusViewportRecovery.test.ts"
    ],
    "why": "Explicit node focus restores readable zoom while fitting the node into the stage; overview remains unchanged."
  }
}
