# 固定等待三连坑

2026-09-09 视频站固定 180s 时只有 6/8 就绪；分镜站 15s 审批探针在模型推理时取消在途请求；点击默认 30s 又把机器排队当失败。

共同根因：把墙钟猜测当业务完成边界。等待必须观察回合终态、审批、任务终态或 DOM 就绪；安全上限交给共享预算 owner，进度记录不能取消请求。阶段 02 用两段模型推理预算共 300s，每 15s 保存状态。

同一 R18 门岗按 AST 扫走查与 sweep 的等待选项，拒绝固定 ≥5000ms；存量按路径和调用内容登记，减少必须同步降基线。三反例红日志在 `docs/plan/sweep-evidence/station-waits-red.log`。传输异常保留原始栈、状态、响应前缀、代理信息与取消来源，避免再靠最终包装错误猜原因。
