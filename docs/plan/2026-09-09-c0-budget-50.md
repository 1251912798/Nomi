# C0 完整版授权预算 ¥50

状态：🚧 进行中。

用户 2026-09-09 批准完整 8 镜 / H3 64 秒，替代原 ¥8 上限。现有预检、逐请求持久化预留、剧本和报告仍声明 ¥8。本次仅同步测试授权到 ¥50，报告与帮助派生共享预算常量，保持模型、镜数、出站白名单、失败保留预留及隔离方式。无生产代码、外部格式或依赖变更。

## 先查别人

- 预算边界已有：`tests/ux/g1/c0-real-budget.mjs:4` 的 BUDGET_CNY 同时约束预检和发送前预留，直接调整授权值。
- 报告已有：`tests/ux/g1/c0-short-film.walk.mjs:33` 记录预算；帮助和报告改为读取既有常量，避免独立数字漂移。
- 模型输入已有：`tests/ux/g1/c0-script.md:25` 固定完整 H3 64 秒；同步授权文字，不改变档位、时长与出站形状。

本次没有引入依赖、通用能力或外部格式，不涉及生态选型或自媒体调研。结论：复用上述既有实现。

验收：¥50 边界与超额拒绝测试、C0 相关测试、contracts 和 gates；打包真实运行由 switch-gate 独立执行记录。回滚：revert 本次测试提交；不得重置已有费用账本。费用全局上限含失败请求和重试，不可按 attempt 清零。
